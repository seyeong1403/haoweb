# CORE_PAGE_MOTION_MAP — 핵심 3페이지 모션 지도 (검토 중 작업안, 2026-07-23)

> 모션 유형은 4개로 제한: **A 텍스트 마스크 리빌 / B 클립 리빌 / C Sticky·Pin / D Hover 마이크로**.
> 공통 규칙: 이징 `cubic-bezier(.16,1,.3,1)` · 숨김은 JS가 붙이는 클래스로만 발생(무JS = 전부 표시) ·
> 모든 유형에 안전망 타이머(2.6s) · `prefers-reduced-motion: reduce` 시 A·B·C 비활성(D는 색 전환만 즉시 적용) ·
> 라이브러리 미도입(바닐라 CSS+JS로 충족 — ScrollTrigger 문법은 scroll 리스너+rAF+300ms 인터벌 보조로 재현, 의존성·성능 관리 우선. 부족해지면 도입 전 이 문서에 기록).

## 공통(3페이지 + 전 페이지 크롬)

| 요소 | 유형 | 트리거 | Desktop | Mobile | reduced-motion | 이유 |
|---|---|---|---|---|---|---|
| 히어로 로드 시퀀스(crumbs→lead→meta→버튼) | A 계열 | 페이지 로드 | 260ms부터 120ms 스태거, 1s 상승 | 동일 | 없음(즉시 표시) | 첫 화면의 등장 리듬(잇다 로드 연출) |
| GNB 링크 | D | hover | 밑줄이 좌→우로 자라는 선 길이 변화 | 해당 없음(모바일 메뉴) | 즉시 상태 | 잇다·도루코의 선 중심 디테일 |
| 메가메뉴·드롭다운 링크 | D | hover | 배경 전환 + padding-left 미세 이동 | 터치 목록(모션 없음) | 즉시 상태 | lift 대체 — 위치 미세 이동 |
| 버튼(.btn) | D | hover | 배경/반전 색 전환 + 화살표(i) 6px 이동 | 터치(전환만) | 즉시 상태 | lift 금지 원칙 |
| 플로팅 문의 필 | C 계열 | scroll(히어로 통과) | 하단 중앙 상승 등장/복귀 | 동일 | 위치 전환만 즉시 | 잇다 contact!·start! 문법 |
| 마퀴 밴드(index) | 장식 | 상시 | 30s 무한 흐름 | 유지(저비용 CSS) | 정지 | 잇다 시그니처 리듬 |

## index.html

| 장면 | 유형 | 트리거 | Desktop | Mobile | reduced-motion | 이유 |
|---|---|---|---|---|---|---|
| 01 Hero h1 | A | 로드(즉시 관찰) | `<br>` 기준 줄 분해 → overflow mask 안에서 줄 단위 상승(110ms 스태거, 0.95s) | 동일(크기만 축소) | 즉시 표시 | 편집형 타이포가 주인공 — 줄바꿈 자체가 디자인 |
| 02 신규/리뉴얼 분할 | D(+보조 리빌 1회) | 뷰포트 진입 10% | 패널 등장 후 hover 시 화살표 8px 이동·라벨 색 전환 | 상하 스택, hover 없음(정보 상시 노출) | 즉시 표시 | 두 경로의 대비가 핵심 — 모션은 보조 |
| 03 제작 방식 | **C(Sticky)** | scroll 연동 | 좌측 큰 메시지 sticky 고정, 우측 6단계가 화면 중심에 온 항목만 하이라이트(나머지 opacity .22) | sticky 해제, 일반 번호 리스트 | 하이라이트 없음(전체 표시) | fade-up 6회 반복 금지 지시 — 스크롤 스토리텔링으로 |
| 04 제작 유형 | D | 뷰포트 진입 | 타일 hover 시 배경·텍스트 반전 + 화살표 이동 | 전체 1열, 설명 상시 노출 | 즉시 상태 | 비대칭 위계(기업·병원 大)가 본체, 모션은 반전만 |
| 05 리뉴얼 X-ray | **B(Clip)** + scrub | scroll 연동 | 표면 레이어가 `clip-path inset` 으로 스크롤 진행률만큼 걷히며 내부 구조(다크) 노출 | 동일(저비용) | 표면 55% 걷힌 정적 상태 | "표면 vs 내부 구조"를 시각으로 — 텍스트 나열 금지 지시 |
| 06 포트폴리오 | **C(Pin)** | scroll 연동(961px+) | 320vh sticky 장면 — 업종 거대 타이포 4종이 진행률로 교체, 01~04 인디케이터 연동, PORTFOLIO. 키워드 글자 라이즈+좌우 드리프트 | 핀 해제 — 헤어라인 리스트로 흐름 | 정적 리스트 | 잇다 프로젝트 목록 전환 문법 |
| 07 전문 서비스 인덱스 | D | 뷰포트 진입 | 행 hover 시 잉크 반전 + 화살표 8px | 1열, 반전 유지 | 즉시 상태 | 6개 서비스를 목록 한 장면으로 — 카드 그리드 금지 |
| 08 플랜·과정·무료 제안 | 보조 리빌 | 뷰포트 진입 | 행 단위 48px 상승 0.95s | 동일 | 즉시 표시 | 정보 밀도가 낮아 강한 모션 불필요 |
| 09 최종 CTA | A(h2) + D | 뷰포트 진입 | 제목 마스크 리빌, 행 hover 시 배경·제목 색 전환+좌측 22px 밀림 | 1열 | 즉시 표시 | CTA 3개의 우선순위를 행 크기로 표현 |

## website.html

| 장면 | 유형 | 트리거 | Desktop | Mobile | reduced-motion | 이유 |
|---|---|---|---|---|---|---|
| 01 서브 Hero h1 | A | 로드 | 줄 단위 마스크 리빌 | 동일 | 즉시 | 페이지 목적(유형 선택) 선언 |
| 02~03 대상·방식 | A(h2) + 보조 | 진입 | 제목 마스크, 목록 소폭 리빌 | 동일 | 즉시 | 빠르게 읽고 지나가는 구간 |
| 04 제작 유형 | D | 진입/hover | index 04와 동일 컴포넌트(반전) | 1열 | 즉시 | 페이지의 핵심 행동(선택) 집중 |
| 05 업종별 차이 | 보조 | 진입 | dl 행 리빌 | 동일 | 즉시 | 표 형태 정보 — 모션 최소 |
| 06 콘텐츠 지원 | D | hover | 서비스 행 반전 | 1열 | 즉시 | index 07과 동일 문법 재사용 |
| 08 플랜 | 보조 | 진입 | 행 리빌 | 동일 | 즉시 | — |
| 09 FAQ | D | 클릭 | +/− 아코디언(details 네이티브) | 동일 | 동일 | 무JS 동작 우선 |

## renewal.html

| 장면 | 유형 | 트리거 | Desktop | Mobile | reduced-motion | 이유 |
|---|---|---|---|---|---|---|
| 01 문제 제기 Hero | A | 로드 | "홈페이지는 있는데, 문의가 없다면." 줄 마스크 리빌 | 동일 | 즉시 | 광고 랜딩 역할 — 첫 문장이 후킹 |
| 02 자가점검 | 보조 | 진입 | 체크 리스트 소폭 리빌 | 동일 | 즉시 | 읽는 속도 방해 금지 |
| 03 X-ray | **B + scrub** | scroll 연동 | index 05와 동일 컴포넌트 | 동일 | 정적 | 페이지 핵심 시각 장면 |
| 04 리뉴얼 범위 | D | hover | 서비스 인덱스 행 반전 | 1열 | 즉시 | 범위 6개를 목록으로 |
| 05 환경 변화(다크) | A(h2) | 진입 | 다크 장면 전환 + 제목 마스크 | 동일 | 즉시 | 장면 대비(화이트→블랙) |
| 06 과정 / 07 플랜 / 08 FAQ | 보조·D | 진입/클릭 | 행 리빌·아코디언 | 동일 | 즉시 | — |

## 검수에서 확인한 안전장치
- 초기 로딩 깜박임: 숨김이 JS 클래스 부여 후에만 발생 → JS 미실행 시 처음부터 전부 표시.
- 모션 전 빈 화면: 안전망 2.6s가 미노출 요소 강제 표시(전 유형).
- 저사양 스크롤: scrub은 rAF 스로틀+최소 계산(요소 5종), transform·clip-path·opacity만 사용(리플로 없음).
